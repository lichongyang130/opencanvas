# OpenCanvas AI 功能路线图 / Backlog

> 当前已有：多模型网关（国内外 + 前台 BYOK）、流式对话、PPT 生成/编辑/导出 PPTX、
> AI 绘图、数据持久化（SQLite）、历史管理（搜索/筛选/置顶/重命名/归档/批量）、场景模板、落地页。
> 下面按优先级列出可继续增加的内容。优先级：P0 必做 / P1 高价值 / P2 中长期。

---

## 一、核心创作能力（补齐对标 HIX/Genspark）

### 1. 深度研究 Deep Research（P0，最该做）✅（演示+真实双模式已交付）
- ✅ 工作台「研究报告」模式：SSE 进度（"正在阅读第 N 个来源"）→ 带 [n] 引用角标报告（Artifact 文档，可下载 MD）→ **一键转 PPT** → 分享只读页
- ✅ 真实联网：Tavily 多路查询（深度/来源数可在设置中心配置）+ 去重 + 模型综述；无 Key 自动降级演示引擎（来源标注「示例」）
- ✅ 工具中心「深度研究」入口（mode=research 直达）；`POST /api/research` 审核（违规 400）+ gateway_usage 记账（demo 也入看板）
- ✅ E2E：`scripts/e2e-research.mjs`（8/8）
- ⬜ 扩展检索源：Exa / 博查 / 秘塔（同构 adapter 接入点已留）、Jina Reader 网页深度阅读、补搜/问答阶梯

### 2. 富文本文档编辑器（P0）
- ✅ 画布内编辑（Markdown 编辑/分屏/预览）、AI 操作（续写/改写/扩写/缩写/纠错）、导出 Word/PDF/Markdown、独立文档库（/docs）
- ✅ TipTap 所见即所得富文本（标题/粗斜体/列表/引用/代码块/撤销重做；Markdown 双向转换，下游 AI/导出链路不变）

### 3. AI 视频生成（P1）✅（演示引擎已交付）
- ✅ 内置演示引擎（零密钥）：提示词散列配色 → 16 帧程序化动画（渐变+光斑+扫描线）→ 纯 Node GIF89a 编码器
- ✅ `/tools/video` 视频工作台：描述/预设/进度条/预览循环播放/下载 GIF；`POST /api/video`（审核+记账 gateway_usage）+ `/api/video/status`
- ✅ E2E：`scripts/e2e-video.mjs`（7/7，含 GIF 字节级校验与用量归属）
- ⬜ 真实文生视频：Kling（FAL，全球）/ 万相 wanx2.1（国内）适配器骨架已留，填 Key 后接视频生成 adapter（分钟级异步任务+进度推送）
- ⬜ 配音/字幕（TTS）、分镜脚本 → 逐镜生成 → 合并 → 视频画廊

### 4. Agent「一句话出整套素材包」（P1，HIX 主打）
- 多节点工作流编排（LangGraph 风格）：
  新品发布 = 研究 → 文案×N → 配图×N → 视频脚本 → PPT
- 节点产物共享上下文（品牌语调 + 研究结论）
- 预置包：产品发布套件、社媒 campaign、网络研讨会、儿童绘本、电商详情页

### 5. 代码生成与预览（P1）
- 生成 HTML/React 单页，iframe 沙箱实时预览
- 落地页/原型/小工具一键生成，截图/部署

### 6. PPT 能力增强（P1）
- ✅ 「配图位」自动调绘图模型填真实图片：单页/批量「AI 配图」，有 Key 用 DALL·E 3 / 万相，否则演示 SVG，imageUrl 随会话持久化
- ✅ 更多版式：时间轴（timeline）/ 对比（compare）/ 流程（process）/ 引言（quote）/ 团队（team），模型 prompt 与解析器同步支持
- ✅ 导出 PDF（浏览器打印逐页输出，@media print 横版）
- ✅ AI 单页重写（真实模型改写选中页，版式可智能适配，旧配图作废可重新生成）
- ✅ 演讲者备注编辑、主题市场、大纲先行

### 7. 图像能力增强（P2）
- ✅ 接入 FLUX：fal.ai FLUX.1 Schnell（文生图）/ Dev（图生图），设置页可配 FAL_KEY、测试连接并拉取模型列表；/api/images 支持 model 指定与 imageUrl 图生图
- ✅ 接入 Seedream 同源通义万相：wan2.7-t2i-flash / wanx2.5-t2i（文生图）、wan2.5-i2i-preview（新版图生图，兼容旧 wanx2.1-i2i-turbo）
- ✅ 图生图：图片工作台「参考图（图生图）」按钮（上传图片附件即作为参考图），画布每张图可一键生成变体（自动选 FLUX dev / 万相 2.5 i2i）
- ✅ 背景移除：每张图「去背景」按钮，优先服务端 remove.bg（REMOVE_BG_API_KEY），未配置时客户端 @imgly WASM 本地 AI（免费、模型 CDN 加载）
- ✅ 绘图模型选择器：图片模式参数行可选「自动 / FLUX / 万相 / DALL·E」，生成成功按模型扣积分
- ⬜ 局部重绘、扩图、Logo/海报/社媒尺寸模板、组图（多视角/多场景）、可灵图像

### 8. 品牌中心 Brand Kit（P2）
- 上传 Logo、色板、字体、品牌语调描述
- 所有 PPT/图/文案自动套用，保持风格一致

### 9. 知识库 RAG（P2）
- ✅ 知识库接入工作台对话：会话可绑定知识库（KbPicker），发送消息自动检索命中片段并注入 system 上下文，回答下方展示引用来源卡片（文档名 + 片段 + 相关度）并可折叠
- ✅ 语义向量化：零依赖 TF-IDF + cosine 语义检索，与关键词命中融合排序（接口不变，可替换 Embedding）
- ✅ 上传网页自动入库、Embedding 向量库（当前复用文档中心：PDF/Word/TXT/MD 上传解析后关联入库）

---

## 二、账户与商业化

### 10. 用户体系（P0 商业化前置）
- ✅ 本地账号版：邮箱+密码注册/登录/登出（scrypt 哈希 + httpOnly cookie 会话，零外部依赖），首页/工作台顶栏 + 首页左侧用户卡片（真实登录态，Portal 弹窗不被遮挡）
- ✅ 会话按用户归属（conversations.userId）：未登录=本地，登录后新会话归本人，列表按身份过滤
- ✅ Google / GitHub OAuth 登录（本地版内置，标准授权码流程 + state 防 CSRF；需在 Google Cloud / GitHub 创建 OAuth 应用并配置 .env 凭据，回调地址见 .env.example）
- ✅ 网关计费端到端验证（本地兼容 Mock 覆盖 OpenAI/DeepSeek/百炼/Anthropic 真实代码路径）
- ⬜ 海外托管版：Clerk/Auth0（多租户、社交登录聚合）
- ⬜ 国内：手机号验证码 + 微信登录
- ✅ 全表隔离（文档/知识库/积分/通知，账号导出与删除同步覆盖）
- ✅ PostgreSQL 版用户体系（完整 Prisma schema + 迁移/导入/冒烟脚本，PG 18.4 实测通过）

### 11. 积分与支付（P1）
- 积分账本真实落库（现在是纯函数）：预扣、结算、流水
- 订阅套餐 + 充值包
- 海外 Stripe；国内微信支付/支付宝（聚合）
- 用量/账单页面

### 12. 团队与协作（P2）
- 团队工作区、成员、共享品牌库/模板
- 产物链接分享（只读页）、评论

### 13. API 开放平台（P2）
- 对外 OpenAI 兼容接口，按 token 计费
- 开发者文档、API Key 管理

---

## 三、基础设施

### 14. 生产化存储与队列（P1）
- ✅ Next.js 14 → 16 + React 19 升级（async params codemod、ESLint 9 flat config、Turbopack dev）
- ✅ SQLite → PostgreSQL（schema + 迁移/导入/冒烟，PG 18.4 实测）
- ✅ Redis + BullMQ 队列抽象（无 Redis 内存队列兜底，/api/health 展示模式）
- ✅ 文件存 Cloudflare R2 / S3（存储抽象：本地默认 / S3 兼容切换，文档全链路实测）

### 15. 模型网关增强（P1）
- ✅ 首个 token 前失败自动重试一次（已开始流式则报错，避免重复计费）
- ✅ 流式请求 HTTP 阶段失败客户端自动重试一次
- ✅ 跨供应商降级（Claude 挂 → 切 GPT）、多 Key 轮询、限流、成本看板；按真实 token 计费已实现

### 16. 可观测性（P2）
- 任务日志、错误追踪（Sentry）、埋点统计

---

## 四、交互与体验

- ✅ Artifact 画布：多产物 Tab（代码预览 ↔ 对话产物）、代码版本历史（最多 10 版可回看）
- ✅ 对话附件：粘贴/拖拽/按钮添加图片与文档（TXT/MD/PDF/DOCX…），文本注入模型上下文、图片 markdown 呈现
- ✅ 语音输入（Web Speech API）/ 消息朗读（speechSynthesis）
- ✅ 移动端响应式（侧栏抽屉、画布全屏）+ PWA（manifest / icon）
- ✅ 新手引导 onboarding（首访 3 步可跳过）、随机灵感、历史分组（今天/昨天/7 天/更早）
- ✅ 消息重新生成 / 编辑重发、顶栏任务标题点击重命名、代码块语法高亮
- ✅ 模板市场：提交模板即上架共享库（可被所有人使用）
- ✅ 产物分享只读页（PPT/文档/图片/报告，含 PPTX 下载与「复制到工作台」）；模板独立分享码（智能体分享已有，可复用）

---

## 五、增长与运营

- （P1）SEO 落地页矩阵：按功能（AI PPT 生成器…）、按模型、按语言
- ✅ 多语言 i18n（中/英；轻量自研，无路由段，cookie 持久化）
- （P2）浏览器插件（划词写作/总结）
- （P2）邀请返利、每日签到送积分
- （P2）埋点 + 转化漏斗分析

---

## 六、合规（做国内必须）

- ✅ 本地内容审核框架：输入拦截 + 输出流式检测（词表/正则可扩展）
- ✅ 图片审核、ICP 备案、算法/生成式 AI 备案（或调用已备案国内模型）、隐私政策、用户协议、数据导出/删除

---

## 建议下一步顺序

1. ✅ **深度研究 + 一键转 PPT**（招牌链路；演示模式零凭据可跑，真实 Key 即接）
2. ✅ **富文本文档编辑器**（产物可编辑，承接研究报告）
3. ✅ **用户登录 + 网关计费 + 全表隔离**（为收费做准备；积分支付/套餐仍待做）
4. ✅ **PPT 自动配图 + AI 视频演示引擎**（演示效果最炸；真实文生视频待 Key）
5. ⬜ **积分支付与用量账单页**（账本/成本看板已就绪，缺支付通道与前端账单页）
6. ⬜ 生产化（PostgreSQL/队列/R2 已抽象）与 **Agent 素材包 / 代码生成预览**
